# Model arguments
model_name_or_path: path to model
tokenizer_name_or_path: path to custom tokenizer
is_rbpe_tokenizer: false

# uncomment this to resize the token embeddings, if you trained your tokenizer using dalla toolkit you will not need to resize the token embeddings
# resize_token_embeddings: true
model_revision: main
torch_dtype: bfloat16
attn_implementation: eager

# Data training arguments
# uncomment this to force a chat template
# chat_template: "{% if messages[0]['role'] == 'user' or messages[0]['role'] == 'system' %}{{ bos_token }}{% endif %}{% for message in messages %}{{ '<|im_start|>' + message['role'] + '\n' + message['content'] + '<|im_end|>' + '\n' }}{% endfor %}{% if add_generation_prompt %}{{ '<|im_start|>assistant\n' }}{% elif messages[-1]['role'] == 'assistant' %}{{ eos_token }}{% endif %}"

# uncomment this to freeze the model
# freeze_parameters: true
# freeze_except: 
#   - embed_tokens
#   - layers.0

dataset_name: path_to_packed_dataset
dataset_num_proc: 12

# SFT trainer config
bf16: true
# dataset_kwargs:
  # add_special_tokens: false  # We already wrap <bos> and <eos> in the chat template
  # append_concat_token: false # No need to add <eos> across samples
do_eval: false
eval_strategy: "no"
gradient_accumulation_steps: 1
gradient_checkpointing: true
gradient_checkpointing_kwargs:
  use_reentrant: false
hub_model_id: test
hub_strategy: every_save
learning_rate: 2.0e-05
log_level: info
logging_steps: 5
logging_strategy: steps
lr_scheduler_type: cosine
max_length: 2048
max_steps: -1
num_train_epochs: 1
output_dir: data/sanity
overwrite_output_dir: true
per_device_eval_batch_size: 1
per_device_train_batch_size: 1
push_to_hub: false
remove_unused_columns: true
report_to:
- tensorboard
- wandb
save_strategy: "no"
seed: 42
warmup_ratio: 0.1